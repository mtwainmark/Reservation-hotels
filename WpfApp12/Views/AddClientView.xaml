<Window x:Class="WpfApp12.Views.AddClientView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:WpfApp12.Views"
        mc:Ignorable="d"
        Title="Добавить клиента" WindowStartupLocation="CenterScreen" Height="270" Width="450">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <TextBox Margin="5" materialDesign:HintAssist.Hint="Фамилия" Text="{Binding Surname}"/>
            <TextBox Margin="5" materialDesign:HintAssist.Hint="Имя" Text="{Binding Name}"/>
            <TextBox Margin="5" materialDesign:HintAssist.Hint="Телефон" Text="{Binding PhoneNumber}"/>
            <TextBox Margin="5" materialDesign:HintAssist.Hint="Паспорт" Text="{Binding Pasport}"/>
            <Button Margin="5" Content="Принять" Click="Accept_Click"/>
        </StackPanel>
    </Grid>
</Window>
